# 更新记录

## 1.5.0 — 2026-09-08

- 支持多会话并行：每个会话独立 pi 进程与流式状态，处理中可自由切换或新开其他会话。
- 后台会话完成或失败时，左树显示未读标识、任务栏图标叠加数字角标、窗口闪烁并发送 Windows 11 系统通知。
- 修复图片粘贴：截图等仅图片剪贴板可直接 Ctrl+V 进入输入框，支持 PNG/位图/文件列表，预览图显示在输入框左上角。
- 会话标题栏显示累计 Token 消耗（本地历史与运行内轮次汇总）。
- 左下角“pi 正在处理”增加动态指示，多会话处理中时侧栏显示未读汇总。
- 右键可标记/取消重点关注，金色星标与左边框在会话列表凸显并本机持久化。
- 发送消息后自动跟随最新内容；会话名称注入的扩展上下文（如 “--- pi-extension-context”）不再误作会话标题。
- 全局搜索结果打开会话后自动定位并高亮第一条匹配消息。

## 1.4.0 — 2026-09-08

- 增加全局全文搜索、匹配片段、来源提示及 Ctrl+Shift+F 快捷键；输入切换可取消旧查询。
- 增加右键/F2 自定义重命名、本机持久化与恢复默认，保持 Pi 会话文件和最近交流时间不变。
- 图标采用用户选择的 B 版紫色色带，去掉黑色外框，右下方增加 harbor；16/24 像素保留简洁 Pi 标识。
- 增加大文件搜索、损坏记录、取消与过期结果、名称持久化测试，以及独立会话 UI 验收。

## 1.3.1 — 2026-09-07

- “回到最新”采用主题紫色圆角按钮，补齐悬停、按下和键盘聚焦状态。
- 侧栏显示分钟粒度的活动时间差和全局“最新”标识；每分钟刷新，悬停显示完整时间。
- 修复会话目录晚于应用创建、持续写入和监听中断时的自动发现；约 5 秒元数据复查兜底，缓存未变化的会话索引。
- 自动刷新保留项目折叠状态、当前会话高亮，合并刷新期间的新变更。
- 增加脱敏截图、Pi 0.84.3 兼容性说明、MIT 许可证和开源贡献资料。
- 增加 Windows CI、Inno Setup 安装器配置、版本标签触发的 GitHub Release 草稿及 EXE/ZIP/SHA-256 附件。

## 1.3.0 — 2026-09-07

- 修复暗色输入框光标，增加 `@` 文件引用、`/` 命令与 skill 选择。
- 增加图片粘贴、缩略图、大图预览、移除和纯图片消息。
- 增加会话草稿隔离、发送失败保留与重新连接，保留历史图片占位提示。

## 1.2.1 — 2026-09-07

- 确定 Pi Harbor 品牌与应用图标。
- 完善大会话滚动、Markdown、模型切换、Token/耗时统计和交互样式。
